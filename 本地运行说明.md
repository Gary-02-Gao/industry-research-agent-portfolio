# 本地运行说明

双击根目录的 **启动项目.command**。脚本会使用本目录 `.venv` 的 Python 3.12，启动后自动打开浏览器；不需要先手工激活环境。

双击 **检查环境.command** 可检查解释器和依赖。这两个双击脚本面向 macOS；首次下载后，请先按下方步骤创建本地环境。

## 行业作品集

- 默认地址：http://127.0.0.1:18092/打开作品集.html；端口占用时自动顺延。
- 打开项目概览、视频、截图和证据文件；支持视频范围请求。
- 本目录是精选代码和成果展示包，不包含完整应用后端、模型服务和数据库。
- 保留启动终端，按 Ctrl+C 停止。已配置 jsonschema、pypdf，可用于相应离线材料检查。

## 环境重建与迁移

`.venv` 是本机环境，移动目录或换电脑后应重建，不应作为投递文件打包。
在项目根目录执行（需先安装 uv）：

```bash
uv venv --python 3.12 .venv
uv pip sync --python .venv/bin/python .launcher/requirements.lock.txt
```

如只需浏览作品集，也可直接打开 `打开作品集.html`；离线统计核验和单元测试只需 Python 标准库，命令见根目录 README。

`MANIFEST.sha256` 覆盖当前提交的全部交付文件（清单自身除外），不包含 Git 元数据、虚拟环境和运行缓存。`02_成果与验证/source-manifest.json` 保留精选核心源码的原始来源哈希。
