# 发布前验证

核验日期：2026-09-23。

- Python 3.12.14 隔离环境及 7 项已安装依赖一致性检查通过。
- 16 项离线单元测试通过；所有交付 Python 文件语法与 JSON 格式检查通过。
- 交付文件 SHA-256、26 份核心源码来源哈希、数据配额和已保存检索/路由统计核验通过。
- 28 处 Markdown / HTML 本地链接有效。
- 一页 PDF、两张界面截图已作视觉检查；视频全程解码通过，并抽取 6 帧检查。
- 启动脚本语法、执行权限及环境检查通过。
- 本地展示服务器主页请求、视频 Range 请求通过；Git 元数据、虚拟环境、运行缓存路径均返回 403。
- 已排除 `.venv/`、`.runtime/`、Python 缓存和本地环境变量文件；文本扫描未发现硬编码凭据或个人机器绝对路径。
- 已修正文件清单检查对本地环境及 Git 元数据的误报，并更新当前交付文件清单。

本仓库是精选代码和成果展示包。以上结果不代表完整应用端到端验证；前端测试需要完整 Node 工程，模型重评需要模型服务和原始数据。本次 HTTP 检查未执行完整浏览器交互回归。截图及演示包含所引用研报的作者和业务联系信息。

本地核验日志和抽帧预览保存在被 Git 忽略的 `.runtime/`，不会上传。
